peipkg-build x86_64 fixture: this is the "fake .so" content.
peipkg-build does not parse ELFs; this byte stream stands in
for a real shared library and exercises the triplet-path,
symlink-chain, ldconfig-side-effect, and signing code paths.
